<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.schooltimetablegenerator.persistence.TeacherRepository">

    <resultMap id="resultMap" type="com.solvd.schooltimetablegenerator.domain.Teacher" autoMapping="false">
        <id property="id" column="id"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName" column="last_name"/>
        <result property="email" column="email"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="teacher.id">
        INSERT INTO teacher (first_name, last_name, email,subject_id)
        VALUES (#{teacher.firstName}, #{teacher.lastName}, #{teacher.email}, #{subjectId})
    </insert>

    <delete id="delete" parameterType="Long">
        DELETE FROM teacher WHERE id=#{id}
    </delete>

    <update id="update" parameterType="com.solvd.schooltimetablegenerator.domain.Teacher">
        UPDATE teacher SET first_name=#{firstName}, last_name=#{lastName}, email=#{email}
        WHERE id=#{id}
    </update>

    <select id="selectById" parameterType="Long" resultMap="resultMap">
        SELECT id, first_name, last_name, email FROM teacher WHERE id=#{id}
    </select>

    <select id="selectAll" resultMap="resultMap">
        SELECT id, first_name, last_name, email FROM teacher
    </select>

</mapper>