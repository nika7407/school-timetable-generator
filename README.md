# school timetable generator 
allows you to generate weekly timetable for multiple classes tghrough the implementation of genetic algorithm variation.

for start set up  *db.properties* file and use *schema* and *base data* in sql to get started after that simply launch the app 
